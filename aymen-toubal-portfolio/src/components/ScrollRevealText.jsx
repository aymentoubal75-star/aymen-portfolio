import {motion,useScroll,useTransform} from 'framer-motion';import {useRef} from 'react';export default function ScrollRevealText({text}){const ref=useRef(null);const {scrollYProgress}=useScroll({target:ref,offset:['start .85','end .35']});return <p ref={ref} className="text-2xl md:text-4xl leading-tight max-w-5xl">{[...text].map((c,i)=><Char key={i} c={c} i={i} n={text.length} p={scrollYProgress}/>)}</p>}function Char({c,i,n,p}){const o=useTransform(p,[i/n,Math.min(1,(i+10)/n)],[.2,1]);return <motion.span style={{opacity:o}}>{c}</motion.span>}
