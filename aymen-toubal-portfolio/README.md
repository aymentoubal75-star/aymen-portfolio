# Aymen Toubal — Web & App Developer Portfolio

Premium React/Vite portfolio based on the provided specification.

## Run

```bash
npm install
npm run dev
```

## Production build

```bash
npm run build
```

Social URLs are centralized in `src/data/socials.js`. The contact form currently performs client-side validation and exposes a `submit` handler ready for EmailJS or another service.
